<!DOCTYPE html>
<html>
  <head>
    <!--Partial HEAD -->
    <%- include('partials/head'); %>
    <!-- Agregar estilos al body  -->
    <link rel="stylesheet" href="stylesheets/style.css">

  </head>
  <body>
   <!--Partial Menú-->
   <%- include('partials/navbar'); %>

   <div class="jumbotron">
    <h1 class="display-4">Bienvenido a KPOPZONE!</h1>
    <p class="lead">Un sitio dedicado al KPOP</p>
    <hr class="my-4">
    
    <a class="btn btn-dark btn-lg" href="/acercade" role="button">Ver más...</a>
  </div>  
</div>
<div id="player"></div>

    <script>
      // 2. This code loads the IFrame Player API code asynchronously.
      var tag = document.createElement('script');

      tag.src = "https://www.youtube.com/iframe_api";
      var firstScriptTag = document.getElementsByTagName('script')[0];
      firstScriptTag.parentNode.insertBefore(tag, firstScriptTag);

      // 3. This function creates an <iframe> (and YouTube player)
      //    after the API code downloads.
      var player;
      function onYouTubeIframeAPIReady() {
        player = new YT.Player('player', {
          height: '360',
          width: '640',
          videoId: '2ips2mM7Zqw',
          events: {
            'onReady': onPlayerReady,
            'onStateChange': onPlayerStateChange
          }
        });
      }

      // 4. The API will call this function when the video player is ready.
      function onPlayerReady(event) {
        event.target.playVideo();
      }

      // 5. The API calls this function when the player's state changes.
      //    The function indicates that when playing a video (state=1),
      //    the player should play for six seconds and then stop.
      var done = false;
      function onPlayerStateChange(event) {
        if (event.data == YT.PlayerState.PLAYING && !done) {
          setTimeout(stopVideo, 1000000);
          done = true;
        }
      }
      function stopVideo() {
        player.stopVideo();
        function onPlayerReady(event) {
  
  event.target.playVideo();
}
      }
    </script>
<div class="jumbotron">
  <h1 class="display-4">COMEBACK BTS Proximamente</h1>
  <p class="lead">El comeback de BTS con el álbum 'BE' podría tener varios significados,<br> muchos fans encontraron diversas conexiones interesantes.</p>
  <hr class="my-4">
  <p>Una teoría apunta a que ‘BE’ podría ser parte de una saga de discos y que el título oficial del comeback sería ‘BE YOURSELF’, agregado las versiones ‘YOUR’ y ‘SELF’,<br>
  no es la primera vez que los chicos presentan este formato, la era ‘Love Yourself’ se publicó bajo la misma idea.</p>
  <img src="/images/com.jpg" width="400" height="250" id="image"/>
  <br>

  <a class="btn btn-dark btn-lg" href="/youtube" role="button">Ver más...</a>
</div>  
</div>   <div class="jumbotron">
  <h1 class="display-4">Icónico baile de Lisa</h1>
  <p class="lead">Las blinks están locas con el baile de Lisa</p>
  <hr class="my-4">
  <p></p>
  <a class="btn btn-dark btn-lg" href="/blinks" role="button">Ver más...</a>
</div>  
</div>

  <!--Partial Scripts-->
  <%- include('partials/script'); %>
  </body>
  <footer>
    <!--Partial Footer-->
     <%- include('partials/footer'); %> 
</footer>
</html>
